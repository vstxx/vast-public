# Optional OpenAI-compatible assistant provider for the side panel.
# Vast never sends browsing data automatically. The user must explicitly trigger an AI action.
VAST_AI_API_KEY=
VAST_AI_BASE_URL=https://api.openai.com/v1
VAST_AI_MODEL=gpt-4.1-mini
