{
  "name": "vast-extensions-hub",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "types": "wrangler types worker-configuration.d.ts --config wrangler.jsonc",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run --config vitest.config.ts",
    "build": "wrangler deploy --dry-run --config wrangler.jsonc --env= --outdir dist",
    "build:signer": "wrangler deploy --dry-run --config signer/wrangler.jsonc --env= --outdir dist-signer",
    "observability:production": "node ../scripts/configure-worker-observability.mjs vast-extensions-hub vast-extensions-hub-signer",
    "dev": "wrangler dev --local --config wrangler.jsonc"
  }
}
